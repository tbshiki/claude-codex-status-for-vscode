{
  "name": "claude-codex-status",
  "displayName": "Claude & Codex Status",
  "description": "Claude Code と OpenAI Codex CLI のレート残量(5時間枠/週枠)をVSCodeのステータスバーに常時表示します",
  "version": "0.1.15",
  "publisher": "local",
  "repository": {
    "type": "git",
    "url": "https://github.com/tbshiki/claude-codex-status-for-vscode.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": ["Other"],
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "claudeCodexStatus.refresh",
        "title": "Claude & Codex: 今すぐ更新"
      },
      {
        "command": "claudeCodexStatus.refreshClaude",
        "title": "Claude & Codex: Claudeのみ更新"
      },
      {
        "command": "claudeCodexStatus.refreshCodex",
        "title": "Claude & Codex: Codexのみ更新"
      },
      {
        "command": "claudeCodexStatus.toggleClaudeMonitoring",
        "title": "Claude & Codex: Claude自動監視を停止／再開"
      },
      {
        "command": "claudeCodexStatus.toggleCodexMonitoring",
        "title": "Claude & Codex: Codex自動監視を停止／再開"
      },
      {
        "command": "claudeCodexStatus.showRawUsage",
        "title": "Claude & Codex: 生の使用状況を表示(診断)"
      }
    ],
    "configuration": {
      "title": "Claude & Codex Status",
      "properties": {
        "claudeCodexStatus.pollIntervalSeconds": {
          "type": "number",
          "default": 300,
          "minimum": 60,
          "description": "残量を取得する間隔(秒)。利用率はゆっくり変化するため、Claude側の負荷を避けて既定は300秒(5分)。短くすると429(レート制限)になりやすくなります(下限60秒)。"
        },
        "claudeCodexStatus.providers.claude": {
          "type": "boolean",
          "default": true,
          "description": "Claude の残量をステータスバーに表示します。"
        },
        "claudeCodexStatus.providers.codex": {
          "type": "boolean",
          "default": true,
          "description": "Codex の残量をステータスバーに表示します。"
        },
        "claudeCodexStatus.claude.credentialsPath": {
          "type": "string",
          "default": "",
          "description": "Claude の credentials.json のパスを明示指定する場合に設定します(通常は空欄でOK)。"
        },
        "claudeCodexStatus.codex.authPath": {
          "type": "string",
          "default": "",
          "description": "Codex の auth.json のパスを明示指定する場合に設定します(通常は空欄でOK)。"
        },
        "claudeCodexStatus.style": {
          "type": "string",
          "enum": ["minimal", "verbose"],
          "default": "minimal",
          "description": "ステータスバーの表示スタイル。minimal は利用率のみ、verbose はリセット残り時間も表示します。"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -w -p ./",
    "package": "node -e \"require('fs').mkdirSync('dist',{recursive:true})\" && vsce package --no-yarn --out dist/ && node scripts/prune-vsix.js"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.4.0",
    "@vscode/vsce": "^3.9.2"
  }
}
